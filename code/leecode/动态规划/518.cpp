#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int change(int amount, vector<int>& coins) {
        if(coins.empty())return 0;
        int si=coins.size();
        vector<vector<int>> dp(si+1,vector<int>(amount+1,0));

        
    }
};